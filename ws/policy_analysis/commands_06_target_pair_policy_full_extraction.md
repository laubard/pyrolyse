# IPv4

```bash
"${PYROLYSE_PATH}/tools/script/scrapping/scrap_pair_policy_complicated_scenarii.sh" \
"${PYROLYSE_PATH}/target/os/${TARGET_NAME}" \
ipv4
```

# IPv6

```bash
"${PYROLYSE_PATH}/tools/script/scrapping/scrap_pair_policy_complicated_scenarii.sh" \
"${PYROLYSE_PATH}/target/os/${TARGET_NAME}" \
ipv6
```

# TCP

```bash
"${PYROLYSE_PATH}/tools/script/scrapping/scrap_pair_policy_complicated_scenarii.sh" \
"${PYROLYSE_PATH}/target/os/${TARGET_NAME}" \
tcp
```